%div
  - if @images.present?
    .row.image-loader
      = render partial: 'image_loader'
  .new-image
    .row
      = render partial: 'form'
  - if @images.present?
    .row
      = render  @images
    .col-md-12
      = paginate @images , window: 2
